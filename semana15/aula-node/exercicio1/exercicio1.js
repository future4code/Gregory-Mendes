// A
/* Dentro do nosso arquivo conseguimos usar a propriedade process.argv para pegar as informações (parâmetros) que passarmos para o terminal. */

// B

/* const name = process.argv[2];
const age =  Number(process.argv[3]);

if (name === "" || isNaN(age)) {
    console.log("Digite os dois parâmetros (nome e idade).");
} else {
    console.log('\x1b[36m%s\x1b[0m', `Olá, ${name}!, Você tem ${age} anos.`);
} */

// C

/* const name = process.argv[2];
const age =  Number(process.argv[3]);

if (name === "" || isNaN(age)) {
    console.log("Digite os dois parâmetros (nome e idade).");
} else {
    console.log('\x1b[36m%s\x1b[0m', `Olá, ${name}!, Você tem ${age} anos. Em sete anos você terá ${age + 7}`);
} */