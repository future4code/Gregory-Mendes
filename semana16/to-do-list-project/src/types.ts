export type User = {
    name: string,
    nickname: string,
    email: string,
};

export type Task = {
    title: string,
    description: string,
    limitDate: string,
    creatorUserId: string
};

